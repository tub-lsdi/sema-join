import HistoryDetailClient from "./HistoryDetailClient";

export default function HistoryDetailPage() {
  // Render a client component that reads the id from the route and fetches data.
  return <HistoryDetailClient />;
}
